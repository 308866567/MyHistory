#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    // if (((used >> i) & 1) == 1) // 判断第 i 位是否是 1
    // used |= 1 << i; // 将第 i 位置为 1
    // used ^= 1 << i; // 使用异或运算将第 i 位恢复
    
    // #include <cstring>
    //   memset(f, 127, sizeof(f)); //取127的后8为2进制()
    // void *memset(void *str, int c, size_t n)
    // 复制字符 c（一个无符号字符）到参数 str 所指向的字符串的前 n 个字符。
    //	在数字没有溢出的前提下，
    //	对于正数和负数，左移一位都相当于乘以2的1次方，左移n位就相当于乘以2的n次方。
    //右移一位相当于除2，右移n位相当于除以2的n次方。这里是取商哈，余数就不要了。
    return 0;
}
